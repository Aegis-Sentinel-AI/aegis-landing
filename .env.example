# ─── NextAuth Configuration ───
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=generate_a_strong_random_secret_here

# ─── Database (Neon PostgreSQL — shared with portal) ───
DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/aegis?sslmode=require
